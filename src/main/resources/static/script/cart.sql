CREATE TABLE CARDS(
    index int,
    main boolean,
    nazwa VARCHAR (100) NOT NULL,

    primary key (index)
);

rabbitmq-plugins enable rabbitmq_web_stomp
rabbitmq-plugins enable rabbitmq_web_stomp
